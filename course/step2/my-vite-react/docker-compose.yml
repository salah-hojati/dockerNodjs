# docker-compose.yml
version: "3.8"

services:
  vite-react:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ./app:/app
      - /app/node_modules
    working_dir: /app

