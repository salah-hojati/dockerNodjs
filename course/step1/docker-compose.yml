version: "3.8"

services:
  vite-react:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - ./app:/app
      - /app/node_modules
    working_dir: /app
    environment:
      - NODE_ENV=development
