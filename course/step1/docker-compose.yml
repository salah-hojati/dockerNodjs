version: "3.8"

services:
  vite-react:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - ./app/:/app  # مپ کامل پوشه react به /app
      - /app//node_modules  # جلوگیری از بازنویسی node_modules
    working_dir: /app
    environment:
      - NODE_ENV=development
